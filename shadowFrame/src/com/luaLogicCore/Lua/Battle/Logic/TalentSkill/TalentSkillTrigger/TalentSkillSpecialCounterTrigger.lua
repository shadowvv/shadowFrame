---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zhaopuyang.
--- DateTime: 2023/2/8 18:19

require "Lib/class"

---@class TalentSkillSpecialCounterTrigger : table
TalentSkillSpecialCounterTrigger = class(nil, 'TalentSkillSpecialCounterTrigger');

function TalentSkillSpecialCounterTrigger:ctor(_battleId)
    ---@type number
    self.battleId = _battleId
end

---@public
---@param _battleObject BattleObjectBase 触发单位
---@param _talentSkillTrigger SkillTrigger 被动技能配置
---@param _talentSkill BattlePlayerTalentSkill 被动技能
function TalentSkillSpecialCounterTrigger:Check(_battleObject, _talentSkillTrigger,_talentSkill)
    ---@type BattleObjCom_Weapon
    local weaponCom = _battleObject:GetComponent(BattleObjectComponentType.Weapon);
    ---@type BattleObjCom_Property
    local propCom = _battleObject:GetComponent(BattleObjectComponentType.Property);
    if not weaponCom or not propCom then
        return 0;
    end
    local current = propCom:GetPropertyByName(weaponCom.curBulletPropName);
    if current == _talentSkillTrigger.parm1 then
        return 1;
    end

    return 0;
end

---@public
function TalentSkillSpecialCounterTrigger:Reset()

end

---被动技能触发类型
---@return number 被动技能触发类型
function TalentSkillSpecialCounterTrigger:GetType()
    return 125;
end

return TalentSkillSpecialCounterTrigger;