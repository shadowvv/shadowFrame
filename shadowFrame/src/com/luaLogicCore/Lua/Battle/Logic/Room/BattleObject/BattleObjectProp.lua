---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zhaopuyang.
--- DateTime: 2021/12/21 15:44
--- 属性数据类

require "Lib/class";

BattleObjectProp = class(nil,'BattleObjectProp');

function BattleObjectProp:ctor()
    self.normalProps = {};
    self.growProps = {};
    self.secondaryProps = {};
    self.capability = 0;
end

-- 添加基本属性
function BattleObjectProp:AddNormalProp(_propKey,_propValue)
    if self.normalProps[_propKey] == nil then
        self.normalProps[_propKey] = 0;    
    end
    self.normalProps[_propKey] = self.normalProps[_propKey]+_propValue;
end

-- 添加成长属性
function BattleObjectProp:AddGrowProp(_propKey,_propValue)
    if self.growProps[_propKey] == nil then
        self.growProps[_propKey] = 0;  
    end
    self.growProps[_propKey] = self.growProps[_propKey]+_propValue;
end

-- 添加随机属性
function BattleObjectProp:AddSecondaryProp(_propKey,_propValue)
    if self.secondaryProps[_propKey] == nil then
        self.secondaryProps[_propKey] = 0;
    end
    self.secondaryProps[_propKey] = self.secondaryProps[_propKey]+_propValue;
end

-- 战力
function BattleObjectProp:SetCapability(_capability)
    self.capability = _capability;
end

return BattleObjectProp;