---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by hejincheng.
--- DateTime: 2022/2/15 16:56

 require "Battle/Logic/Room/BattleResult/BattleResultFunction"
 require "Battle/Logic/Room/BattleResult/BattleResult"

-- 枚举
---@type BattleResult[] 枚举映射
BattleResultEnum = {
    ---@type BattleResult 没有结果
    NO_RESULT = BattleResult.New(0, BattleResultFunc_Demo);
    ---@type BattleResult 平局
    EQUAL = BattleResult.New(1, BattleResultFunc_Demo);
    ---@type BattleResult 胜利
    WIN = BattleResult.New(2, BattleResultFunc_Demo);
    ---@type BattleResult 失败
    FAIL = BattleResult.New(3, BattleResultFunc_Demo);
}

-- 枚举映射
---@type BattleResult[] 枚举映射
local battleResultIdMap = {};

-- 初始化BattleResult映射
for _, value in pairs(BattleResultEnum) do
    battleResultIdMap[tostring(value:GetId())] = value;
end

-- 根据id获取枚举
---@type function
---@param _id number
---@return BattleResult
function GetBattleResultById(_id)
    return battleResultIdMap[tostring(_id)]
end