---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by jiangxuechen
--- DateTime: 2022/2/8 15:15

require "Battle/Logic/Room/Component/BehaviorProcessor"

---@class ServerBehaviorProcessor : BehaviorProcessor
ServerBehaviorProcessor = class(BehaviorProcessor, 'ServerBehaviorProcessor')

function ServerBehaviorProcessor:ctor(_battleId)
    self.battleId = _battleId
    self.battleRoom = GetBattleRoom(self.battleId)
end

-- log
function ServerBehaviorProcessor:CheckLuaBehavLog(...)
	--LogTools.LogByLevel(LogLevel.log, "CheckLuaBehav", "ServerBehaviorProcessor", ...)
end


-- 目前AI主导权在服务器
-- 将来如果确定改为host机制，这里检查host开关
function ServerBehaviorProcessor:CheckLevelAIHost()
    return not self.battleRoom.indieGame
end

-- 检查怪物AI主导权
function ServerBehaviorProcessor:CheckMonsAIHost()
    return not self.battleRoom.indieGame
end

return ServerBehaviorProcessor;