---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by jiangxuechen
--- DateTime: 2022/04/09 18:51
--- Describe:
---

---@class AreaTriggerTemplate_Box : table
AreaTriggerTemplate_Box = class(AreaTriggerTemplateBase, "AreaTriggerTemplate_Box")

---@public
function AreaTriggerTemplate_Box:CheckAABB(_createTime, _nowTime, _nowPos, _checkPos)
	local _size = self:__CountSizeScale(_createTime, _nowTime)
	
	if _size.x > 0 then
		if _checkPos.x < _nowPos.x - _size.x or _checkPos.x > _nowPos.x + _size.x then return false end
	end
	
	if _size.y > 0 then
		if _checkPos.y < _nowPos.y - _size.y or _checkPos.y > _nowPos.y + _size.y then return false end
	end
	
	if _size.z > 0 then
		if _checkPos.z < _nowPos.z - _size.z or _checkPos.z > _nowPos.z + _size.z then return false end
	end
	
	return true
end

return AreaTriggerTemplate_Box