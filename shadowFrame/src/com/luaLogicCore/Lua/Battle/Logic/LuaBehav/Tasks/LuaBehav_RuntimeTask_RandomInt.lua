---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by jiangxuechen
--- DateTime: 2022/01/21 11:51
--- Describe: int随机值, 闭区间，随机结果乘以scale后存储到指定id的变量中
---

---@class LuaBehav_RuntimeTask_RandomInt : table
LuaBehav_RuntimeTask_RandomInt = class(LuaBehav_RuntimeTask, "LuaBehav_RuntimeTask_RandomInt")

-- 
function LuaBehav_RuntimeTask_RandomInt:OnStart()
	self:__OnStart()
	
	local _configTask = self.configTask
	local _min = _configTask.min or 0
	local _max = _configTask.max or 1
	if _min > _max then
		_min = _max
		_max = _configTask.min or 1
	end
	local _scale = _configTask.scale or 1
	
	self.behavTree.variableDic[_configTask.saveId] = math.random(math.floor(_min), math.floor(_max)) * _scale	
end

return LuaBehav_RuntimeTask_RandomInt