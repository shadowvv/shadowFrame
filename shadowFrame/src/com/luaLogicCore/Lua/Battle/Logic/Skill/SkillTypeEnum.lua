---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by hejincheng.
--- DateTime: 2022/4/21 17:44

 require "Battle/Logic/Skill/SkillType"


-- 枚举
---@class SkillTypeEnum:table
---@type SkillType[] 枚举映射
SkillTypeEnum = {
    -- 1、普攻
    ---@type SkillType 枚举
    NORMAL = SkillType.New(1),
    -- 2、主动
    ---@type SkillType 枚举
    ACTIVE = SkillType.New(2),
    -- 3、极奏
    ---@type SkillType 枚举
    JI_ZOU = SkillType.New(3),
    -- 4、被动天赋
    ---@type SkillType 枚举
    PASSIVE = SkillType.New(4),
    -- 5、式杖镜像技能
    ---@type SkillType 枚举
    WEAPON_DEFAULT_SKILL = SkillType.New(5),
}

-- 枚举映射
---@type SkillType[] 枚举映射
local skillTypeIdMap = {};

-- 初始化SkillType映射
for _, value in pairs(SkillTypeEnum) do
    skillTypeIdMap[tostring(value:GetId())] = value;
end

-- 根据id获取枚举
---@type function
---@param _id number
---@return SkillType
function GetSkillTypeById(_id)
    return skillTypeIdMap[tostring(_id)]
end

-- 根据ID获取技能类型的描述名
---@public
---@return string
function GetSkillTypeNameById(_skillType)
    if 1 == _skillType then
        return "普攻"
    elseif 2 == _skillType then
        return "主动"
    elseif 3 == _skillType then
        return "极奏"
    elseif 4 == _skillType then
        return "被动天赋"
    elseif 5 == _skillType then
        return "式杖镜像技能"
    else
        return "没找到技能类型"
    end
end