---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zhaopuyang.
--- DateTime: 2023/4/3 15:28

require "Lib/class"
require "Battle/Logic/Skill/SkillBaseNode"

---@class SkillCallSummonSkill : SkillBaseNode
---@field nodeId number 节点id
---@field nextNodes SkillNodeExitData[] 子节点
SkillCallSummonSkill = class(SkillBaseNode, 'SkillCallSummonSkill');

---@param _nodeId number 节点id
---@param _once boolean 只执行一次
---@param _nextNodes SkillNodeExitData[] 子节点
---@param _specialExit SkillNodeExitData[] 特殊出口
function SkillCallSummonSkill:ctor(_nodeId, _once, _nextNodes, _specialExit,_SummonId,_SummonSkillId)
    self.nodeId = _nodeId;
    self.once = _once;
    self.nextNodes = _nextNodes;
    self.specialExit = _specialExit;
    self.summonId = _SummonId;
    self.summonSkillId = _SummonSkillId;
end

---执行技能节点
---@param _actionStateManager ActionStateManage 行为管理器
---@param _dt number 更新时间
---@param _netId number 单位id
---@param _skillModel SkillModel 技能配置
---@return boolean,table 是否执行结束,执行行为输出数据
function SkillCallSummonSkill:DoAction(_actionStateManager, _dt, _netId, _skillModel)
    local battleRoom = GetBattleRoom(_actionStateManager.battleId);
    battleRoom.battleUnitManager:OnUnitCallSummondSkill(_actionStateManager:GetUnit():GetNetId(),self.summonId,self.summonSkillId);
    return true, nil;
end

---返回节点类型
---@public
---@return number 节点类型
function SkillCallSummonSkill:GetType()
    return 69;
end

return SkillCallSummonSkill;